# KIOSK Frontend — 待填入

平板自助點餐前端,使用 React 18 + Vite + TypeScript + Tailwind CSS,目標解析度 1024x768 直式。

主要職責:
- 多層級菜單瀏覽(`GET /api/menu`)
- 購物車、客製化選擇
- 結帳 + 會員登入(`POST /api/auth/verify`)
- 建立訂單(`POST /api/orders`)
- 顯示取餐號碼與 QR Code(供手機掃描)

啟動(待補):
```bash
npm run dev
```
